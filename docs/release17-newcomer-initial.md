# Initial UI-only newcomer attempt — not a completed acceptance run

The first fresh-context UI-only navigator found Arcade (1.8 s), Pause (2.2 s),
returned once at 31.9 s, purchased at 110.4 s, completed the relay repair chain
and returned at 137.2 s. At the timed-delivery contract, the compass continued
to request containers after its cargo requirement had been met. The base mission
settlement was correct, but its progress/return guidance was not.

The attempt was stopped rather than counted as a completed newcomer run.
The speed/no-damage return predicates and visible timed-cargo progress were fixed.
See `release17-newcomer.json` for the replacement fresh-context attempt.
