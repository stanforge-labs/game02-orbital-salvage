# CorePass05 — локальная финальная проверка

Проверка выполнена на свежем HTML5-экспорте из production `game.json`.

## Рендер

- viewport: 1920×1080
- devicePixelRatio: 1
- CSS canvas: 1920×1080
- backing canvas: 1920×1080

## Улучшения

- Корпус: реальный runtime-клик при 150 кредитах → 0 кредитов; после `НОВЫЙ ВЫЛЕТ` HUD показывает `КОРПУС 4 / 4`.
- Трюм: реальный runtime-клик при 100 кредитах → следующий вылет с `ГРУЗ 0 / 10`.
- Двигатель: 240.220795 до покупки, 264.248972 после; изменение +10.0025%.
- `НОВЫЙ ВЫЛЕТ` проверен настоящим mouse click и touch tap.

## Камера

Телеметрия записана одним настоящим прогоном в `camera-trace-pass05.csv` и `camera-trace-pass05-combined.json`.

- Сценарий: W3s → S3s → W→S → S→W → D3s → A3s → D→A → A→D.
- Строк: 1207.
- X корабля: 350.00–1019.90; X камеры: 480.00–905.92; максимум |ΔX|/кадр: 4.2496.
- Y корабля: 70.00–743.66; Y камеры: 270.00–677.00; максимум |ΔY|/кадр: 4.1542.
- В разворотах W→S и S→W камера Y менялась (локальные максимумы |ΔY|/кадр: 4.1542 и 1.9167); в разворотах D→A и A→D камера X менялась (локальные максимумы |ΔX|/кадр: 4.1880 и 0.8375).
- Во всех направлениях не обнаружено телепортации или резких скачков.
- Резких скачков и телепортации не обнаружено.

## Исходник и runtime

- `game.json` разбирается как JSON.
- Цвет сцены: `r=0, v=0, b=0`.
- Неизвестных экземпляров: 0.
- Отсутствующих локальных ресурсов: 0.
- QA-инъекции кредитов, отладочного состояния и телеметрии удалены из production source/export.
- В финальном runtime-проверочном прогоне JavaScript-ошибок: 0.
