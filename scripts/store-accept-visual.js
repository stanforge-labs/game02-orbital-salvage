// Records hashes AFTER the reviewer has opened every deliverable. Not an automated visual test.
if(!process.argv.includes('--confirm-all-full-size-reviewed'))throw Error('Full-size review confirmation required');
const fs=require('fs'),crypto=require('crypto'),selection=require('../release/store_media/source/selection.json');
const files=['icon/icon_512.png','cover/cover_800x470.png',...[256,128,64].map(n=>`icon/icon_preview_${n}.png`),...selection.screenshots.map(s=>`screenshots/${s.platform}/${s.output}`),'store_media_contact_sheet.png'];
const hash=p=>crypto.createHash('sha256').update(fs.readFileSync('release/store_media/'+p)).digest('hex').toUpperCase();
fs.writeFileSync('release/store_media/source/visual-review.json',JSON.stringify({reviewedAt:new Date().toISOString(),reviewer:'Codex: each final PNG opened with view_image at full size; previews and contact sheet inspected',method:'Explicit visual review attestation, not an automated image-quality classifier',checks:['No browser/system UI or DEV','Readable ship/loot/hazards and HUD','Distinct chosen mechanics and compositions','No invented screenshot overlays or scene edits','No alternate game title','Icon at 512/256/128/64'],files:Object.fromEntries(files.map(p=>[p,hash(p)])),videoSHA256:hash('video/gameplay_16x9.mp4'),videoReview:'52 decoded frames at 0.5-second cadence and all edit boundaries inspected; complete browser playback and complete FFmpeg decode; silent'},null,2));
